# #Manual Trigger
# ->Go to jenkins dashboard ->select the job ->click build now
# ->useful for adhoc runs(testing a fix immediatley)
#
# #SCM(Source code management) Trigger
# ->Configure job to run whenever code is commited/pushed to Github/Bitbucket
# ->In job ->Configure ->Build Trigger ->select