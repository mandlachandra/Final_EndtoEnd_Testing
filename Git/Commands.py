# clone a repository  = git clone<>
# check status == git status
# Add changes to staging area == git add .
# commit changes  == git commit -m "Added my first commit"
# push changes to remote branch == git push origin feature/testcase
# pull latest changes from remote  == git pull origin master
# create a new branch  == git checkout -b "chandra"
# merge branches == git merge feature
# view commit history  == git log --oneline

#wrong commit in vpn server in github how could be revert that code
# git log --online
#
# abc added wrong code
#
# git revert abc code
#
# git push origin main

#real time logs
# git log --online--decorate --graph --all