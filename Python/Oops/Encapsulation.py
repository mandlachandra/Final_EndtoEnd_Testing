#Encapsulation in Page Objects(POM)
# Locators and interaction methods are bundled inside the page class
# Tests cannot directly touch locators--they only call methods

from selenium.webdriver.common.by import By

